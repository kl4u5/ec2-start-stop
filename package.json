{
  "name": "ec2-start-stop-cdk",
  "version": "1.0.0",
  "type": "module",
  "description": "AWS CDK project for automated EC2 instance start/stop scheduling",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:lambda": "cd lambda && pnpm run build",
    "build:all": "pnpm run build:lambda && pnpm run build",
    "clean": "rm -rf dist && cd lambda && rm -rf dist",
    "watch": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "cdk": "cdk",
    "deploy": "pnpm run build:all && cdk deploy",
    "deploy:dev": "pnpm run build:all && cdk deploy --profile kbj-aws-dev",
    "deploy:prod": "pnpm run build:all && cdk deploy --profile sps-production",
    "diff": "pnpm run build:all && cdk diff",
    "synth": "pnpm run build:all && cdk synth",
    "bootstrap": "cdk bootstrap",
    "destroy": "cdk destroy",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "pnpm run format:check && pnpm run lint && pnpm test",
    "fix": "pnpm run format && pnpm run lint:fix",
    "precommit": "node scripts/precommit.mjs",
    "precommit:simple": "pnpm run format && pnpm run lint:fix && pnpm run build:all && pnpm run test",
    "precommit:bash": "bash scripts/precommit.sh 2>/dev/null || (echo \"❌ Pre-commit checks failed. Fix the issues above and try again.\" && exit 1)",
    "prepare": "husky"
  },
  "keywords": [
    "aws",
    "cdk",
    "ec2",
    "automation",
    "scheduling"
  ],
  "author": "Klaus Bjorn Jensen <kl4u5.j3n53n@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@typescript-eslint/eslint-plugin": "^8.40.0",
    "@typescript-eslint/parser": "^8.40.0",
    "@vitest/coverage-v8": "^3.2.4",
    "aws-cdk": "^2.1025.0",
    "eslint": "^9.33.0",
    "husky": "^9.1.7",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.868.0",
    "@aws-sdk/client-ssm": "^3.864.0",
    "aws-cdk-lib": "^2.211.0",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  }
}
